- set_title @page.name

#chat
  %input{ type: 'text', placeholder: 'Nachricht schreiben' }
  %a{ href: '#' } Verlauf löschen

#user.hide{ 'data-name' => @user.name }
#page.hide{ 'data-token' => @page.token, 'data-name' => @page.name }

#online-users{ style: 'didsplay:none' }
  %span
    Online:
  .users

.container
  .row
    .span12

      #components

        -# Text
        .component
          .title-bar
            %h3
              .title-text.pull-left
                Text
              .actions.pull-right
                %a.toggle-component{ href: '#' }
                  %i.icon-chevron-up
                  %i.icon-chevron-down{ style: 'display:none' }

          .content-bar
            #ace-editor
          .footer

        -# Termin
        .component
          .title-bar
            %h3
              .title-text.pull-left
                Termin
              .actions.pull-right
                %a.toggle-component{ href: '#' }
                  %i.icon-chevron-up{ style: 'display:none' }
                  %i.icon-chevron-down

          .content-bar{ style: 'display:none' }
            TODO Termin
          .footer

        -# Abstimmung
        .component
          .title-bar
            %h3
              .title-text.pull-left
                Abstimmung
              .actions.pull-right
                %a.toggle-component{ href: '#' }
                  %i.icon-chevron-up{ style: 'display:none' }
                  %i.icon-chevron-down

          .content-bar{ style: 'display:none' }
            TODO Abstimmung
          .footer

        -# ToDo Liste
        .component
          .title-bar
            %h3
              .title-text.pull-left
                ToDo Liste
              .actions.pull-right
                %a.toggle-component{ href: '#' }
                  %i.icon-chevron-up{ style: 'display:none' }
                  %i.icon-chevron-down

          .content-bar{ style: 'display:none' }
            TODO ToDo Liste
          .footer

#edit-page-title.modal.hide.fade{ tabindex: -1 }
  .modal-body
    %input{ type: 'text' }
  .modal-footer
    %a.cancel{ href: '#', class: 'btn' } Abbrechen
    %a.save{ href: '#', class: 'btn btn-primary' } Speichern